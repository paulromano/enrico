name: SCALE Tests
on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout
      uses: actions/checkout@master
      with:
        submodules: true

    - name: Configure
      run: |
        echo $(pwd)
